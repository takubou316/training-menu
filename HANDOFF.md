# HANDOFF.md（training-menu）

- **最終更新日時**: 2026-08-12（Codex更新）
- **変更主体**: Codex（自己ベスト更新ハイライトを実装）

このファイルは長期の設計方針（`CLAUDE.md`）ではなく、今どこまで進んでいて次に何をするかの短期メモ。ClaudeとCodexの併用運用における最初の試運転として、training-menuを対象に選んだ（詳細はルート`CLAUDE.md`の「Claude/Codexの併用について」、`Obsidian Vault\メモ\Claude-Codex併用ルール.md`を参照）。

## 現在の目的

training-menuを、Claude/Codex併用運用の最初の試運転プロジェクトにする。まずは小さく分かりやすい機能を1つCodexに実装してもらい、HANDOFF.md・Codex側作業日記（`作業日記codex`）・Claude側`yarukoto`の連携が想定どおり回るかを確認する。

## 現在の実装状態

- 種目データベース・メニュー自動生成・記録・4種のタイマー・進捗グラフ（種目別推移／総挙上量推移）は実装済み（詳細は`CLAUDE.md`参照）。
- 進捗グラフは`js/workout-log.js`の`exerciseProgressSeries(exerciseId, exerciseMeta, limit)`（189行目）が、種目ごとの実測値（重量／回数／保持秒数、種目タイプにより出し分け。`js/ui.js`の`progressMetricInfo`）を時系列で返す。ウォームアップセット（`isWarmup: true`）は既存のあらゆる集計（履歴・進捗・総挙上量）から一貫して除外されている。
- 「自己ベスト（PR）更新のハイライト表示」をCodexが実装済み。セットの完了チェック時に、保存済み履歴の`exerciseProgressSeries`から過去最高を取得し、今回の値が厳密に上回る場合だけ該当セットにバッジを表示する。重量種目・自重種目・保持時間種目に対応し、有酸素種目・ウォームアップセットは対象外。

## Codexに任せる作業

**自己ベスト（PR）更新のハイライト表示**を実装した。

- セットを「完了」として記録確定した時点（`js/workout-log.js`の`finalizeSession`まわり、または記録画面でのセット完了処理）で、その種目の過去最高値（重量・回数・保持秒数。種目タイプに応じて`progressMetricInfo`と同じ基準を使う）を、今回の値が更新したかを判定する。
- 過去最高値の参照元は既存の`exerciseProgressSeries`（またはそれに準ずる集計）を使い、新しい集計ロジックを別途作らない。
- 更新していたら、記録画面（該当セット・種目カード付近）に「🏆自己ベスト更新！」のような分かりやすい表示を出す。既存の`.set-row`/種目カードのスタイルと調和する見た目にする（`UI_UX_GUIDELINES.md`のトーンに合わせる）。
- ウォームアップセット（`isWarmup: true`）は自己ベスト判定・比較対象の両方から除外する（既存の集計と同じ扱いに合わせる）。

## 今回の実装結果

- `js/workout-log.js`: `exerciseProgressSeries`の既存結果を参照する`isPersonalRecord`を追加。初回記録は表示せず、今回値が過去最高と同値の場合も表示しない。
- `js/ui.js` / `js/app.js`: 本セットの完了チェック時に該当セットへ「🏆 自己ベスト更新！」を表示。完了を外すと表示を消す。
- `css/style.css`: 既存のアクセント色・セット行のトーンに合わせたバッジスタイルを追加。
- 有酸素種目は今回の対象外。表示はセット完了時、未完了に戻すまで残る仕様とした。

## 今回やらない作業

- ChatGPT案にあった「先月より総重量+12%」「通算100回目のトレーニング」「今年初めて◯◯kg達成」「3週間連続トレーニング」等、自己ベスト以外の節目演出（ロードマップには載っているが今回のスコープ外）。
- 記録一覧／グラフのカレンダー統合（ロードマップに設計確定済みだが別項目、今回は触らない）。
- ロードマップの他の項目（UX見直し、体重推移グラフ等）全般。
- ロードマップ・アイデアまとめ・Claude側`作業日記`の編集（Claudeが管理するため）。
- Git commit・push（Claudeが一括管理するため、依頼があるまで行わない）。

## 完了条件

- ある種目で過去最高（重量・回数・保持秒数のいずれか、種目タイプに応じた基準）を上回るセットを記録すると、その場で「🏆自己ベスト更新！」等の表示が出る。**確認済み。**
- 過去最高を更新していない通常のセットでは表示が出ない（誤検知しない）。**確認済み。**
- そのセットが初回記録（過去データが無い種目）の場合にエラーにならず、かつ誤って「自己ベスト更新」と表示されない。**確認済み。**
- 既存の記録・タイマー・進捗グラフの動作に影響を与えない。
- PCプレビューで一度動作確認する。**確認済み。** スマホ実機確認は今回未実施。

## 確認方法・結果

1. ローカルHTTPサーバーでPCプレビューを起動し、ダンベルカールを10kgで保存後、11kgを完了して「🏆 自己ベスト更新！」表示を確認。
2. 同値の10kgでは表示されず、PR表示済みの完了を外すと表示が消えることを確認。
3. 履歴のない初回10kgでは表示されず、エラーにならないことを確認。
4. Nodeによるロジックテストで重量・自重回数・保持秒数、有酸素除外、ウォームアップ除外、初回記録を確認。既存の進捗グラフ・記録保存のコードパスは変更していない。

## 未確認事項・既知の問題

- **初回記録（過去データなし）の扱い**: 初回は表示しない。比較対象がないため。
- **表示タイミング**: そのセットの「完了」チェック時点で即座に表示する。
- **表示の出しっぱなし時間・消し方**: 次のアクションまで残し、完了を外したら消す。
- **有酸素種目**: 今回は対象外。時間・距離による有酸素PRは別仕様として未実装。
- **スマホ実機確認**: 未実施。必要なら別途確認する。

## 次にやること

- Claude側で差分レビュー後、必要ならロードマップ・共有メモへの反映方針を判断する。
- スマホ実機確認が必要かを判断し、必要なら表示幅・タップ操作を確認する。
- Git commit・pushは未実施。Claude側の管理方針に従う。

## 参照

- [CLAUDE.md](CLAUDE.md)（アプリ概要・アーキテクチャ）
- [Obsidian Vault/アイデアまとめ/03 トレーニングメニューアプリ/ロードマップ.md](../../../Obsidian%20Vault/アイデアまとめ/03%20トレーニングメニューアプリ/ロードマップ.md)（「自己ベスト（PR）更新のハイライト表示」項目）
- Obsidian Vault/作業日記/03 トレーニングメニューアプリ（Claude側の作業日記）
- `js/workout-log.js`（`exerciseProgressSeries`, `finalizeSession`）、`js/ui.js`（`progressMetricInfo`）
