# HANDOFF.md（training-menu）

- **最終更新日時**: 2026-08-14（Claude更新）
- **変更主体**: Claude（設計相談〜実装〜PCプレビュー確認〜実機バグ修正〜見た目統一）

## 現在の目的

1. モード選択画面の最上部に、週間プランの「今日の予定」を出す（完了、実機バグ修正込み）。原点回帰UX見直しの一項目。
2. 上記の実装過程で指摘された、アプリ内の「控えめな操作」（折りたたみ・下線リンク）の見た目バラつきを`.ghost-pill-btn`に統一（完了）。

## 実装内容

### 1. 今日の予定パネル

`#today-focus-section`（画面最上部）を`js/ui.js`の`renderTodayFocus`が埋める。週間プラン未作成なら
何も出さない。今日が実行可能な予定なら「今日は○○の日です」＋「始める」を表示しモードカードを
折りたたむ、休みの日なら「今日は休みの日です」の一言だけ表示してカードは開いたまま。
設計判断の詳細は[CLAUDE.md](CLAUDE.md)の「モード選択画面：今日の予定を最上部に出す」節を参照。

**実機バグとその修正（同日中に発見・解消済み）**: 初回push後、ユーザーがiPhoneで確認したところ
「他のメニューを作る」を開いていないのに中の2枚のカードが表示されたままだった。原因は
`.mode-choice`の`display:flex`指定がブラウザ標準の`<details>`非表示処理を上書きしていたため
（PCのChromiumプレビューでは再現せず実機のみで発覚）。`:not([open])`の明示ルールで解消済み。
詳細はCLAUDE.mdの「既知の罠」参照。

### 2. 控えめな操作ボタンの見た目統一

「他のメニューを作る」の見た目について、箱スタイル(`.wu-cd-toggle`)→下線リンク(`.text-link-btn`風)→
どちらも「スタイリッシュじゃない」と指摘があり、mcp__visualize（Imagine）で複数案を提示して
ユーザーが選んだのが現在の`.ghost-pill-btn`（輪郭だけの角丸pillボタン）。気に入られたため、
アプリ内の同種の箇所すべてに展開した。対象箇所・変更内容の一覧はCLAUDE.mdの
「控えめな操作ボタンの見た目統一」節を参照。ウォームアップ/クールダウンだけは「本編の1セクション」
という性質の違いから`.section-toggle`という別スタイル（左にアクセント縦線＋太字見出し＋開閉シェブロン）。

### 3. 今日の予定パネルの再調整

`.ghost-pill-btn`統一後、今日の予定パネル(`.today-focus-panel`)だけがaccent-dimのベタ塗り箱で
浮いて見えると指摘があり、`.section-toggle`と同じ「箱を作らず左にアクセント線」スタイルに変更した
（「始める」ボタンの主要感を残すため、線の色は`.section-toggle`よりも明るい`--accent`）。

`service-worker.js`のキャッシュ名は今回の一連の変更で`v9`→`v10`（バグ修正）→`v11`（見た目統一）→
`v12`（今日の予定パネル再調整）まで更新した。

## 確認方法（Claudeが実施・完了）

- `node --check`で`js/app.js`・`js/ui.js`の構文エラー無しを都度確認。
- PCプレビュー（`training-menu-alt`設定、ポートは都度自動割り当て。別セッションが`training-menu`
  本来のポートを使用中だったため）で、localStorageに直接テストデータを入れて確認:
  - 今日の予定パネル: 週間プラン未作成／実行可能な予定あり／休みの日の3パターン、「始める」の
    遷移まで確認
  - 控えめな操作ボタン統一後、対象7箇所すべてを実際にその画面まで遷移して見た目を確認
    （モード選択の2箇所、週間プランパネルの2箇所、自分で作る画面、記録カード内、カレンダー詳細）
  - PCプレビュー側でService Workerの古いキャッシュを掴んで変更が反映されない事象が複数回発生した
    （`unregister()`+`caches.delete()`で都度クリアして対処。実機側でも同じ注意が必要）
- **実機（iPhone）で確認済み**: モード選択画面最上部（今日の予定パネル`.today-focus-panel`が
  左アクセント線スタイルになっていること、「他のメニューを作る」が輪郭pillになっていること）を
  スクリーンショット＋「良い感じだと思う」の言葉で確認済み（v12）。
- **実機で未確認のまま**: 週間プランパネルの2箇所（編集する／＋新しいプラン、ほかのプランを見る）、
  自分で作る画面の「保存した組み合わせから読み込む」、記録画面の`.section-toggle`
  （ウォームアップ/クールダウンのシェブロン回転）、記録カード内の`.detail-toggle-btn`
  （コンパクト版が窮屈に見えないか）、カレンダー詳細の`.jump-links`。いずれもPCプレビューでは
  確認済みだが、実機では未確認。何か違和感があれば追加で報告してもらう想定。

## 未確認事項・既知の問題

- 上記の通り、モード選択画面以外（記録画面・自分で作る画面・週間プラン編集画面）の見た目統一が
  実機でどう見えるかは未確認。
- 原点回帰の残り項目（チュートリアル作成、「今日やることを最上部に」以外のChatGPT案、動画/デモ機能）
  は未着手のままObsidian Vaultのロードマップに残っている。

## 参照

- [CLAUDE.md](CLAUDE.md)（「モード選択画面：今日の予定を最上部に出す」「控えめな操作ボタンの見た目統一」節）
- [AGENTS.md](AGENTS.md)
- Obsidian Vault「アイデアまとめ\03 トレーニングメニューアプリ\ロードマップ.md」
