# HANDOFF.md（training-menu）

- **最終更新日時**: 2026-08-13（Claude作成）
- **変更主体**: Claude（タスク整理）

このファイルは長期の設計方針（`CLAUDE.md`）ではなく、今どこまで進んでいて次に何をするかの短期メモ。ClaudeとCodexの併用運用の一環（詳細はルート`CLAUDE.md`の「Claude/Codexの併用について」、`Obsidian Vault\メモ\Claude-Codex併用ルール.md`を参照）。**このリポジトリには`AGENTS.md`もあるので先に読んでください。**

## 現在の目的

継続日数（何日連続でトレーニングしているか）を記録・表示する機能を追加する。ロードマップの「これから」に載っていた項目（自己ベスト表示・休憩タイマー修正の2タスクとは別の、新しいタスク）。

## 現在の実装状態

- トレーニング記録は`js/storage.js`の`loadHistory()`等でlocalStorage(`training-menu:history`)に保存されている。セッションには`date`（実施日時）が記録済み。
- 継続日数（何日連続でトレーニングしているか）を計算・表示する仕組みは**まだ無い**。
- 参考実装: 同じtakutolibrary配下の`guitar-practice/index.html`に、プロフィールごとの連続日数を記録する`getStreak()`/`streakKey()`のような仕組みが既にある（`guitar-streak-<プロフィール名>`キーで`{last, count}`を保存、同日は据え置き・前日から連続なら+1・間が空いたら1にリセット、というロジック）。同じ考え方をtraining-menu用に移植できる。

## Codexに任せる作業

- 記録確定（`finalizeSession`でトレーニング記録が保存されるタイミング）ごとに、継続日数を更新する。ロジックはguitar-practiceの`getStreak`と同じ考え方でよい: 同日なら据え置き、前日から連続なら+1、間が空いたら1にリセット。
- 継続日数を保存するキー名・保存場所は`js/storage.js`の既存の命名パターンに合わせる。
- モード選択画面（`screen-mode`）か記録一覧画面など、目立ちすぎない場所に「🔥N日連続」のような形で表示する（既存のUIトーンに合わせる。過度に目立たせない）。
- 記録が1件も無い状態（初回）でエラーにならないこと。

## 今回やらない作業

- 自己ベスト表示・休憩タイマー修正（既に完了済み、このタスクとは無関係）。
- ロードマップ・アイデアまとめ・Claude側の作業日記の編集。
- Git commit・push。

## 完了条件

- トレーニングを記録した日の連続日数が正しくカウントされる（同日/連続/間隔ありの3パターンで動作確認）。
- 初回記録（継続日数データが無い状態）でエラーにならない。
- 既存機能（記録・タイマー・PR表示など）に影響がない。

## 確認方法

1. PCプレビューで1回記録し、「1日連続」等の表示が出ることを確認する。
2. localStorageの日付を手動で書き換える、またはNodeでロジック単体テストし、連続/リセットの分岐を確認する。

## 未確認事項・既知の問題

- 表示する場所・文言の細かいデザインは未確定。既存UIと調和する範囲でCodexの判断に委ねる。曖昧で判断がつかない場合は先に確認すること。

## 参照

- [CLAUDE.md](CLAUDE.md)
- [AGENTS.md](AGENTS.md)
- `js/storage.js`（`loadHistory`等）、`js/workout-log.js`（`finalizeSession`）
- 参考実装: `guitar-practice/index.html`の`getStreak`/`streakKey`
