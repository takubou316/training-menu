# HANDOFF.md（training-menu）

- **最終更新日時**: 2026-08-13（Claude更新）
- **変更主体**: Codex（実装）＋Claude（PCプレビュー動作確認・後片付け）

## 現在の目的

記録一覧とグラフ画面を統合し、カレンダー表示を追加する本実装。

## 現在の実装状態

- `index.html`の`#screen-history`と`#screen-progress`を`#screen-record`へ統合し、記録／グラフの上位タブと、カレンダー／リストの表示切り替えを追加した。
- カレンダーは実データを日付単位でグループ化し、複数セッション、日付移動、前後の記録日ジャンプ、記録スタンプ、空状態導線に対応した。
- `js/ui.js`で既存の履歴カード生成ロジックを`buildSessionCardHtml(session)`へ切り出し、cardio・holdBased・ウォームアップ除外・体重換算・RPE・削除ボタンを記録タブ内で再利用するようにした。
- `js/app.js`の画面遷移・削除イベント・空状態導線・記録完了後の遷移を新しい記録画面へ接続した。記録完了直後は今日を選択して開く。
- `css/style.css`へ試作由来のカレンダー、日詳細、タブ、表示切り替え関連スタイルを追記した。
- `assets/stamp-record.svg`を追加し、`service-worker.js`のキャッシュ名を`training-menu-v8`へ更新して新規アセットを追加した。
- ボトムナビは5個から4個（メニュー作成／週間プラン／記録／豆知識）になった。
- 試作ファイル`prototype-history-calendar.html`と既存アセットは変更せず残置している。

## 確認方法（Claudeが実施・完了）

- `node --check js/ui.js`・`node --check js/app.js`とも構文エラー無し。
- PCプレビュー（`serve.bat`、ポート8081）で以下を実機ブラウザ操作で確認:
  - ボトムナビが4個（メニュー作成／週間プラン／記録／豆知識）になっている
  - 記録0件時の空状態文言＋「＋ メニューを作る」ボタンが実際にメニュー作成画面へ遷移する
  - 「自分で作る」→記録確定で、記録画面に戻り当日にスタンプが付き、日の詳細にセッションカードが表示される
  - カレンダー⇄リスト表示の切り替え、グラフタブ（記録2回未満時の案内文含む）が動作する
  - 個別削除ボタン→確認モーダル→削除で、空状態に正しく戻り🔥バッジも消える
  - `assets/stamp-record.svg`が200 OKで読み込まれる、コンソールエラー無し
- Codex実行の副産物`codex-implementer-output.txt`（サマリーのみのログ）を削除済み。

## 未確認事項・既知の問題

- 実機（スマホ）でのタップ操作・PWAオフラインキャッシュ更新（v8反映）確認は未実施。次回実機確認時に見ること。
- 複数セッション同日ケースはPCプレビューでは未検証（コードレビューでロジックは確認済み）。

## 参照

- [CLAUDE.md](CLAUDE.md)
- [AGENTS.md](AGENTS.md)
- `prototype-history-calendar.html`
