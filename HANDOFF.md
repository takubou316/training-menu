# HANDOFF.md（training-menu）

- **最終更新日時**: 2026-09-16（Claude更新）
- **変更主体**: Claude（設計相談〜実装〜PCプレビュー確認〜実機バグ修正〜見た目統一〜入力方式の見直し）

## 2026-09-16: ヘッダーに更新(⟳)ボタンを追加

**きっかけ**: standaloneでホーム画面に追加したPWAにはブラウザのURLバー・更新ボタンが無く、
最新コードを取ってきたい時に「一度ホーム画面から削除して開き直す」しか手段が無かった、
というユーザー指摘。

`index.html`の`.app-header`をflexにし、右上に`#hard-reload-btn`(⟳、`.icon-btn`共通見た目)を
追加。`js/app.js`の`hardReload()`が、Service Workerのキャッシュ(オフライン用フォールバック、
`caches.delete`)を明示的に消してから`location.reload()`する(fetch自体は既にネットワーク
優先`cache:'no-store'`のため理屈上は`location.reload()`単体でも最新化されるはずだが、
念のためキャッシュも消してから再読み込みする)。押している間はボタンを回転させて
処理中であることを示す。

ローカルの簡易サーバーで、押すとJSファイル一式が再取得されること・コンソールエラーが
無いことを確認済み。**実機での確認はまだ**。

## 2026-09-16: テーマ(配色)切り替え機能を追加

**きっかけ**: 上記のUI/UX監査・Before/Afterモックアップの相談の流れで、ユーザーから
「黒×明るい緑という配色がダサく感じる」という指摘があった。バイアスというより、
ここ数年の「AIが作ったダークモードSaaS」でよく見る量産型の配色である、という評価で
一致した。mcp__visualizeで複数の配色案(インディゴ・アンバー・モノトーン・セージグリーン・
ピンク系数種)をBefore/After形式で見比べてもらい、最終的に以下で決定した:

- **既定(基本形)**: C案のアンバー(オレンジ、`#ff9d5c`)
- **設定から選べる残り3色**: D案のモノトーン(オフホワイト、`#e8e6e1`)、G案のダスティローズ
  (`#d98fa6`)、ユーザーが送ってくれたスクリーンショットの色(A/Eの中間、セージグリーン
  `#6fc09b`)

**実装**:
- `css/style.css`: `:root`(および`[data-theme="amber"]`)を新しい既定配色にし、
  `[data-theme="mono"]`/`[data-theme="rose"]`/`[data-theme="sage"]`で
  `--bg`/`--surface`/`--surface-2`/`--text`/`--text-dim`/`--accent`/`--accent-dim`/
  `--border`を丸ごと差し替える。`--danger`(削除等の意味色)は全テーマ共通で固定。
  各テーマの`--surface`等は元の配色の明度差(bgからの相対的な明るさの増分)をそのまま
  引き継ぐ形で算出し、色相だけ変えて統一感を保っている。
- `index.html`: `<head>`に即時実行スクリプトを追加し、CSS読み込み前に保存済みテーマを
  `data-theme`属性へ反映(FOUC対策)。記録画面の「その他の設定」内に4色の丸ボタン
  (`.theme-swatch`)によるテーマピッカーを追加。
- `js/storage.js`: `loadTheme`/`saveTheme`を追加(`training-menu:theme`キー、
  不正な値が入っていた場合は既定の'amber'にフォールバック)。
- `js/app.js`: `applyTheme(theme)`(data-theme属性の切り替え・保存・
  `<meta name="theme-color">`の更新・選択中スワッチの見た目更新をまとめて行う)と
  `wireThemePicker()`を追加、`init()`の先頭で呼ぶ。
- **実装時に見つけた小さな見た目の問題**: 選択中スワッチの枠を`border-color`で
  表現すると、モノトーンテーマは丸自体の色(`#e8e6e1`)と枠の色(`--text`、`#f5f4f2`)が
  近すぎて選択中かどうか分かりにくかった。`outline`+`outline-offset:3px`に変更し、
  丸と枠の間に必ず背景色の隙間ができるようにして解消(どのテーマでも枠と背景のコントラストが
  保証される)。

ローカルの簡易サーバーで4色すべてへの切り替え・選択中インジケーターの見た目・
リロード後の永続化(`data-theme`・`<meta name="theme-color">`とも)を確認済み。
**実機での確認はまだ**。

## 2026-09-16: 全画面のUI/UX監査（Codexレビュー）で見つかった実害バグを修正

ユーザーから「全画面をCodexにレビューさせて、デザイン面でもっと良くしたい」と依頼があり、
Claude自身の視覚的レビュー(全8画面・約12モーダルをBrowserペインでスクリーンショット確認)と、
`codex-reviewer`によるコードレベル監査(index.html・style.css・ui.js・app.jsの全文)の両方を実施。
Codexからは31件の指摘(アクセシビリティ・一貫性・CSSデッドコード等)を受けたが、量が多いため
ユーザーの判断で「まず実害のある不具合だけ今直す」方針にし、以下を修正した
(アクセシビリティ関連の指摘(数字ホイールのキーボード操作不可、chip内チェックボックスの
支援技術からの隠蔽、モーダルのdialogロール・フォーカス管理不足等)は今回は見送り、
別途対応する)。

1. **有酸素種目の「完了」チェックが押しても見た目が変わらない**: `js/ui.js`の
   `buildCardioExerciseCardHtml()`で「完了」ラベルが`<span class="done-toggle-pill">`で
   囲まれておらず、筋トレのセットと違って緑のピル表示に切り替わらなかった。囲むよう修正。
2. **セット完了時、自己ベスト(🏆)バッジが一緒に消える**: `css/style.css`の
   `.set-row.is-done .set-pr-badge { display:none; }`が、達成した直後にバッジを
   隠してしまい、事実上お祝い表示が機能していなかった。このセレクタを削除。
3. **セット完了時、重量情報も一緒に消える**: `.set-row.is-done .slider-field`が
   スライダーごと重量ラベルも隠すが、代わりに表示される`.set-row-summary`
   (`setRowSummaryText()`)には重量が含まれていなかった。重量スライダーがある種目
   (自重種目は対象外)だけ、サマリーの先頭に重量を追加。
4. **カレンダーの「今日」の枠線が、記録が無い日は残ったまま**: CLAUDE.mdに書いた
   「今日の強調はバッジのみ、マス全体の枠線は使わない」という設計意図と実装がズレて
   いた(記録がある日だけバッジ化され、記録が無い今日はマス全体の枠線だけが残っていた)。
   記録の有無に関わらず日付をバッジ表示にし、マス全体の枠線指定は削除。
5. **`.ghost-pill-btn`(控えめボタンの共通クラス)の背景が塗りつぶしになっていた**:
   「輪郭だけ・塗りつぶし無し」という設計意図(css/style.cssのコメント)と実装がズレて
   いたため、背景を透明に修正。
6. **種目ピッカーを開くと、豆知識のカテゴリ選択が無関係に解除される**: `.picker-filter-btn`
   クラスを種目ピッカーと豆知識のカテゴリ絞り込みの両方で共有しており、
   `openExercisePicker()`のリセット処理がページ全体の`.picker-filter-btn`を対象に
   していたため。`#exercise-picker-modal`内に限定するよう修正(`wireExercisePicker()`の
   クリックリスナー登録も同様に限定)。
7. **生成メニューから全種目を削除すると、開始ボタンが有効なままalert()が出る**:
   `renderMenu()`で種目0件時に空状態メッセージを表示し、開始ボタンを`disabled`にする
   よう修正。あわせて`.primary-btn:disabled`のスタイル(グレーアウト)も追加。

**見送った項目(意図的な既存仕様と判明)**: Codexは「セット行の重量スライダーだけ他画面より
小さい」ことも不一致として指摘したが、実際には`css/style.css`のコメントに
「大きくて邪魔に見えるという実機指摘があり、セット行内だけ意図的に縮小した(2026-08-14)」
という経緯が明記されており、修正対象から除外した。**Codexのようなコード単体でのレビューは、
過去の実機フィードバックに基づく意図的な仕様変更を「不整合」と誤検知することがあるため、
必ずコード中のコメント・変更履歴を確認してから反映すること。**

ローカルの簡易サーバー(python)で、上記7件すべてをJS直接実行で動作確認済み(有酸素完了ピルの
色、PRバッジ・重量サマリーの表示継続、カレンダー今日バッジ、ghost-pill-btnの背景、
豆知識カテゴリ選択の保持、空メニュー時の無効化ボタン)。**実機での確認はまだ**。

視覚的レビュー(Claude自身)で見つかった「画面全体の視覚的階層(メリハリ)が弱い」という
所見については、mcp__visualizeでモード選択画面のBefore/Afterモックアップを1件作成し
ユーザーに提示済み。他画面への展開は今回のスコープ外(まず実害バグの修正を優先)。

Codexの残り24件(アクセシビリティ・CSSデッドコード候補・モーダルの意味付け不足等)は
今回未対応。次に着手する時は、このセッションのCodexレビュー結果(31件、優先度付き)を
参照すること。

## 2026-09-16: 有酸素タイマーの「計測中の表示」と「記録される時間」がズレる不具合を修正

**きっかけ**: ユーザーが実機(iPhone、公開URL`takubou316.github.io`)でウォーキングの計測をスクリーン
ショット付きで報告。「計測中 00:16」まで進んだのに、計測を終わった後にスライダーへ反映されている
時間が「0分7秒」しかない、というズレがあった。

**当初、原因は2つ(いずれもDOM経由の間接的な値反映に頼っていたこと)と考えて両方修正したが、
Codexレビューの結果、確実に実害があったと言えるのは2番目のラベルバグのみで、1番目は
「防御的には正しい改善だが、報告された症状の直接の原因だったとは確認できない」との指摘を受けた
（下記「Codexレビュー」参照）。**

1. **(未確認の仮説だったもの)`ex.duration`自体がズレる、という説**: `js/cardio-timer.js`の
   `updateCardioTimer`は従来、`slider.value`への代入＋`dispatchEvent('input')`だけでモデル
   (`currentSession.exercises[exIndex].duration`)への反映を`js/app.js`の`handleCardioLogInput`に
   任せていた。記録中セッションの復元(`restoreActiveSessionIfAny`、2026-09-15追加)直後の
   最初のティックは`#log-content`のinputリスナーがまだ登録される前に発火するため、このイベントが
   誰にも届かず反映がその回だけ抜け落ちる。ただし`currentActiveMs()`は`ex.duration`ではなく
   `accumulatedActiveMs`と絶対時刻`segmentStartedAt`から経過時間を再計算するため、次のティックや
   停止時に自然と追いつく設計になっており、これ単体で経過時間が失われ続けるわけではない
   （Codexレビュー指摘、当初「抜け落ちが積み重なり大きくズレる」と書いていたのは推測だった）。
   **対応**: `applyCardioDurationValue(exIndex, value)`という共通関数を新設し(`js/app.js`)、
   モデル更新・ラベル表示・推定カロリー表示をこの関数が直接行うようにし、`updateCardioTimer`が
   これを直接呼ぶことでイベント伝播への依存自体は無くした（実害が確認できた修正ではないが、
   防御的な改善として残す判断）。
2. **(確認できた実際の原因)`.slider-value`ラベル(「時間 X分Y秒」の表示)がそもそも常に更新されない
   バグ**: 調査中に
   発見。ラベル取得が`target.parentElement.querySelector('.slider-value')`になっていたが、
   2026-08-14に追加された「トラック両脇の範囲表示」(`.slider-track-row`)のせいで、実物の
   スライダー(重量・有酸素の時間/距離)では`target.parentElement`が`.slider-track-row`になり、
   兄弟要素の`.slider-value`(`.slider-label`の中)へ届かなくなっていた。数字ホイール(回数・RPE・
   体重)は`<input>`が`.slider-field`の直接の子のため無事だった。同じ理由で壊れていた箇所を
   `js/app.js`内3箇所（`applyCardioDurationValue`・`handleCardioLogInput`の距離欄・
   `handleLogInput`の重量欄）すべて`target.closest('.slider-field')`に統一して修正した
   （`positionSliderBubble`は元から`.closest`を使っており無事だった）。

ローカルの簡易サーバー(python)で、計測中の`ex.duration`・ラベル・モーダル表示の3つが常に
一致すること、計測開始直後のリロード復元でも即座に一致すること、計測終了後も一致することを
JS直接実行で確認済み。**実機(iPhone、公開版)でも2026-09-16にユーザーが確認済み**:
2分4秒計測後の「時間 2分4秒」表示・推定消費カロリー(約8kcal、体重×MET×時間の計算とも
つじつまが合う)ともにズレなし。

**Codexレビュー(read-only、コミット`86eedf2`が対象)で見つかった点、追加対応済み**:
- 上記1番目の「未確認の仮説」だった点の指摘（記載済み）
- `restoreCardioTimer`が保存された`exIndex`を検証せずに復元しており、壊れたスナップショットや
  将来の仕様変更で現在のセッション内容と噛み合わなくなった場合、対応するスライダーが存在しない
  まま有酸素タイマーだけが動き続ける「宙に浮いた」状態になりうる → `restoreActiveSessionIfAny`で
  復元先が実在する有酸素種目であることを確認してから`restoreCardioTimer`を呼ぶよう修正
- 同様に、`restoreActiveSessionIfAny`のtry/catchが`currentSession`等はリセットするが、
  `restoreCardioTimer`側で例外が起きた場合に`activeCardioTimer`とモーダル表示が宙に浮いて
  残ることがある → catch節で`stopCardioTimer()`による後片付けも試みるよう追加
- (低優先度、簡素化)通常のtickごとに`applyCardioDurationValue`と`persistActiveSessionSnapshot`が
  `dispatchEvent`経由でも二重に呼ばれてしまう(実害は無いが無駄)点の指摘 → スライダーの塗り更新
  (`updateSliderTrackFill`)を合成`input`イベント経由ではなく直接呼ぶよう変更し、二重呼び出しを
  解消
- 長時間(120分＝スライダーの上限を超える)有酸素計測では、モーダル表示は上限なく伸びるが記録される
  時間は120分に丸められる、という既存の仕様（今回のバグとは無関係、対応は見送り）の指摘あり

## 2026-09-15: 上記の記録中セッション復元機能にCodexレビューで見つかった3件のバグを修正

新しい記録中セッション復元機能（直下の節）についてCodexへ設計レビューを依頼（read-only、
`codex-reviewer`経由）。指摘された3件（いずれも実コードで再現条件を確認済み、優先度P2）を
すべて修正しコミット済み。

1. **`?quickstart=`の再発火**: 記録中セッションを復元する時、URLパラメータの除去
   （`history.replaceState`）自体も丸ごとスキップしていたため、そのセッションを完了した後に
   再度リロードされると、消し忘れたquickstartパラメータが意図しない新規セッションを開始して
   しまっていた。`consumeEntryParams()`としてURL読み取り・除去だけを独立させ、復元の有無に
   関わらず必ず一度だけ呼ぶように分離（`js/app.js`）。
2. **認証確認前の記録が同期対象から漏れる**: 起動直後、`initSupabaseAuth()`の最初のセッション
   確認(`getSession()`、非同期)が終わる前に復元したセッションを「記録して終了」すると、
   `isCloudSyncActive()`が`currentSupabaseSession`未設定のため偽になり、`queueSessionForSync`が
   その記録を静かにキューへ積まず取りこぼしていた。`js/sync.js`に`authInitDone`/
   `waitForAuthInit()`を追加し、同期を選択済みなのに認証確認未了の間は`queueSessionForSync`が
   確認完了を待ってから判定し直すようにした。
3. **画面移動でセッション開始時刻が失われる**: 記録画面はボトムナビ（メニュー作成／週間プラン／
   記録／豆知識）がいつでもタップ可能で、タップすると（`currentSession`が生きたままでも）
   無条件に`stopSessionTimer()`が呼ばれ`sessionStartTime`が`null`になっていた。その後アプリを
   バックグラウンドにすると`sessionStartTime: null`のスナップショットで上書きされ、次回起動時に
   `startSessionTimer(null)`が現在時刻から再スタートしてしまい、それまでの経過時間が最終的な
   `durationSec`から失われていた。`js/session-timer.js`に`pauseSessionTimerDisplay()`
   （インターバルだけ止めて`sessionStartTime`は消さない）を新設し、ナビゲーション時は
   `stopSessionTimer()`の代わりにこちらを使うよう変更。

あわせて簡素化1件（`js/cardio-timer.js`の`updateCardioTimer`が毎秒2回同じ内容を保存していた
重複除去）と、防御的な修正1件（`restoreActiveSessionIfAny`を`try/catch`で囲み、壊れた/想定外
形式のスナップショットで例外が起きても`init()`の残りのイベント配線を止めないようにした）も実施。

ローカルの簡易サーバーで、有酸素タイマー計測中にボトムナビで別画面へ移動→そのままリロード→
記録画面が復元され、`sessionStartTime`が保持されたまま「記録して終了」した記録の`durationSec`が
ナビゲーション・リロードを挟んだ実時間と一致することを確認済み（78秒）。認証確認前の同期漏れ
(#2)はコード上のレビューのみで、実際のSupabaseログインを使った実地確認はまだ。

## 2026-09-15: 記録中セッションが消える重大バグを修正

ウォーキング等の有酸素タイマーで計測中に他アプリへ長時間切り替えると、戻ってきた時に
アプリが勝手にリロードされ、計測中の時間も記録も失われる（完了もできない）という重大な
不具合をユーザーが報告。原因は、記録中の`currentSession`（トレーニング記録全体）と
`activeCardioTimer`（有酸素タイマーの状態）が両方ともJSのメモリ上にしか存在せず、
localStorageへの保存は記録確定時(`finalizeSession`)まで一切行われていなかったこと。
ブラウザ/PWAには真のバックグラウンド実行の権限が無く、OSがバックグラウンドのタブ/PWA
プロセスを終了させることがある（特にウォーキング中に地図アプリ等へ長時間切り替えた場合）。
復帰時にページが丸ごとリロードされると、上記のメモリ上の状態がすべて失われていた。

**修正**: 記録中は`js/storage.js`の新規キー`training-menu:active-session`へ随時スナップショット
保存するようにした（`js/app.js`の`persistActiveSessionSnapshot`、呼び出しは記録開始時・
各セットや有酸素スライダーの入力時・有酸素タイマーの毎秒ティック時・`visibilitychange`が
`hidden`になった時・`pagehide`時）。起動時(`init()`)に未完了のスナップショットがあれば
`restoreActiveSessionIfAny`が記録画面と有酸素タイマー(計測中/休憩中含む)をそのまま復元する。
有酸素タイマーの開始時刻(`segmentStartedAt`)は絶対時刻のまま保存しているため、既存の
「開始時刻だけを覚えておき現在時刻との差分で経過時間を計算し直す」設計（`js/cardio-timer.js`
冒頭コメント）にそのまま乗る形で、リロードで空いた時間も自動的に経過時間へ反映される。
記録確定時(`handleFinishWorkout`)にスナップショットをクリアする。

rest-timer/hold-timerは数十秒〜数分程度の短時間な操作であり、リロードに巻き込まれても
「もう一度セットを完了にする／もう一度計測ボタンを押す」程度の実害で済むため対象外にした。

ローカルの簡易サーバーによる動作確認で、計測中にページを丸ごとリロード→記録画面と
有酸素タイマーの両方が復元され、経過時間もリロードで空いた時間を含めて正しく継続することを
確認済み。**実機（iPhone、実際に他アプリへ切り替えてOSに殺されるケース）での確認はまだ**。
詳細はCLAUDE.mdの「タイマーまわりの方針」に追記予定。

**この環境固有の注意（2026-09-15判明、同日中に解決）**: このPC(`takutolibrary`ルートの
`.claude/launch.json`)の`training-menu`設定は`python`コマンドを前提にしているが、当初この
マシンの`python`/`python3`はWindowsストアの実行エイリアス（未インストール状態のスタブ）を
指しており動作しなかった。`node`/`npm`もPATH上に見当たらなかった。動作確認はPowerShellの
`System.Net.HttpListener`で代替の簡易静的サーバーをその場で用意して行った。その後ユーザーの
許可を得てwingetでPython 3.12・Node.js LTSを実体インストール済み（Node.js自体は既に
インストール済みだったことが判明）。ただしClaude Codeアプリのプロセスは起動時点のPATHを
子プロセスへ引き継ぐため、インストール後もアプリ再起動までは反映されない（Windowsの一般的な
仕様）。詳細は記憶`project_training_menu_local_server_python_missing`参照。

## 2026-09-08: Codexレビューで見つかったバグを修正（オフラインキューのentryId化、URL排他処理）

ショートカット機能一式についてCodexへ設計レビューを依頼（diffベース、ファイル編集なし）。
8件の指摘のうち影響度の高い2件を修正（コミット`376e347`、対のgame-daily-manager側は
`42649a7`）。詳細は[CLAUDE.md](CLAUDE.md)の「クラウド同期中の削除の伝播」「クイックスタート」
節・game-daily-manager/HANDOFF.mdの該当エントリ参照。

1. **オフラインキューの成否記録がlocalId単位だったバグ**: 同じlocalIdにupsertとdeleteが
   両方キューに積まれる（オフライン中に記録→すぐ削除）と、片方が成功しただけでもう片方の
   失敗した操作までマージ時に誤って取り除かれてしまっていた。各エントリに一意な`entryId`
   (`crypto.randomUUID()`)を持たせ、成否の記録をentryId単位に変更
2. **`?quickstart`と`?view=record`が同時に付いたURLでの競合**: `maybeStartQuickstart()`/
   `maybeOpenRequestedView()`を`maybeHandleEntryParams()`に統合し、quickstart優先で
   排他的に処理するようにした

PCプレビューで両パラメータ同時指定時にquickstartが優先されること、ゲストモードでの
記録→今日のデータ削除フローに例外が出ないことを確認済み。

見送った指摘（低優先度）: 複数タブでの同期競合（1人1台1タブ想定のため許容、詳細は
[CLAUDE.md](CLAUDE.md)の「既知の制約」節）。

## 2026-09-08: ローカル削除をクラウドにも伝播（game-daily-manager側の「達成」表示が残る不具合を修正）

ユーザーから「今日のデータを削除した時、全体管理画面の達成表示は未達成に戻るのか」と質問があり、
調べたところ削除がSupabaseへ伝播していないと判明（ローカルのlocalStorageしか消しておらず、
既に同期済みのtraining_sessions行が残ったままになっていた）。`js/sync.js`に
`queueSessionDeleteForSync`/`deleteSessionFromSupabase`を追加し、削除時も既存のオフライン
キュー経由でSupabase側を後追いで削除するようにした。詳細は[CLAUDE.md](CLAUDE.md)の
「記録データの削除」節参照。PCプレビューではゲスト状態での削除フローに例外が出ないことを
確認済み。実機でも「今日のデータを削除する」→クラウド側も削除→全体管理画面が未達成表示に
戻ることをユーザーが確認済み。

## 2026-09-08: 「今日のデータを削除する」を記録画面に追加

「記録データをすべて削除する」のほかに、今日の分だけをまとめて削除できる操作を「その他の設定」に
追加した。ウォーキングのクイックスタートのように「とりあえず開始してみる」使い方が増え、間違えて
記録した1日分をまとめてやり直したい場面向け。詳細設計は[CLAUDE.md](CLAUDE.md)の
「記録データの削除」節参照。PCプレビューで動作確認済み（今日のセッションのみ削除され他日に
影響しないこと、確認モーダルの文言を確認）。

## 2026-09-08: game-daily-managerのショートカットから直接記録画面へ飛ぶ「クイックスタート」を追加

`game-daily-manager`（別リポジトリ）に追加中の筋トレショートカット機能（ユーザーが自由に登録した
タスクを特定の有酸素種目に紐づけ、ワンタップで記録開始できる機能）の受け皿。URLに
`?quickstart=<exerciseId>`が付いていたら、通常の「自分で作る→種目を追加→生成」フローを短絡し、
いきなり記録画面（該当種目のカードのみ）まで遷移する。

- 新規関数`js/app.js`の`maybeStartQuickstart()`。`init()`の最後（`restoreLastSettings()`・
  `initSupabaseAuth()`より後）で呼ぶ。`findExerciseById`で種目を引き、`type !== 'cardio'`や
  未知のidなら何もせず通常のモード選択画面のまま（現時点では有酸素種目のみ対応、ショートカット側の
  `link_type`が将来`exercise`以外に拡張されても安全に無視できる）
- `currentMenu`を`main: [buildCustomCardioPlan(exercise)]`だけで直接組み立て、`handleStartWorkout()`を
  そのまま呼んでいる（`buildCustomCardioPlan`・`handleStartWorkout`とも既存の「自分で作る」フローの
  関数をそのまま再利用、新規ロジックは追加していない）。ウォームアップ/クールダウンは空の状態
  （`{ general: '', dynamic: [], staticStretch: [] }`/`{ static: [], general: '' }`）で固定 —
  `buildWarmupHtml`/`buildCooldownHtml`がnullを受けるとそのままクラッシュするため、`customWarmup`/
  `customCooldown`の初期値と同じ最小形にしている
- **再読み込み時の誤発火防止**: 判定直後に`history.replaceState`でURLからクエリパラメータを
  除去する（ページ遷移は起きない）。これにより、記録画面滞在中にブラウザをリロードしても同じ
  クイックスタートが再度実行されて別セッションが二重生成される、という事故を防いでいる
- PCプレビューで確認済み: `?quickstart=walking`→ウォーキングの記録画面へ直接遷移→「記録して終了」
  まで実行→記録タブに反映（連続日数カウントも更新）。`?quickstart=bench_press`（有酸素でない種目）
  では何も起きず通常のモード選択画面が表示されることも確認。コンソールエラーなし
- **実機（iPhone）で確認・2件の不具合を発見して修正済み（同日、コミット`5220e72`）**:
  1. ウォームアップが一個も出ない → 「有酸素単体には要らないだろう」という独自判断で
     `warmup`を空にしていたのが誤りだった。`menu-generator.js`にはpattern:'cardio'向けの
     専用ウォームアップ（「ごく軽いペースで3〜5分」）が既に用意されており、通常の
     「自分で作る」で有酸素だけ選んでも表示される内容だった。`buildWarmupAndCooldown()`を
     そのまま呼ぶよう修正し、通常フローと同じ結果になるようにした
  2. game-daily-manager側の「達成」済みショートカットをタップすると、確認したいだけなのに
     毎回新しいクイックスタートセッションが始まってしまう → `?view=record`を新設
     （`maybeOpenRequestedView()`）し、達成時はこちらへ誘導するようgame-daily-manager側を
     変更（対の変更はコミット`6eef780`）
- 対になるgame-daily-manager側の実装状況は`game-daily-manager/HANDOFF.md`参照

## 2026-09-07: 実機で「クラウド同期が有効表示なのに一切同期されない」重大バグを発見・修正

フェーズ6の実機総合検証中、ユーザーがGoogleログインしても記録が同期されない不具合が発覚。
詳細・修正内容は[CLAUDE.md](CLAUDE.md)の「実機で発見した重大バグと修正」節を参照。
実際のSupabaseアクセストークンをローカル環境に注入して再現・修正確認済み。

## 2026-09-07: 記録データの実同期(フェーズ4)を追加

`js/workout-log.js`の`finalizeSession`から`js/sync.js`の`queueSessionForSync`を呼び、記録を
Supabaseの3テーブルへ複製する処理を実装。詳細設計・既知の制約は[CLAUDE.md](CLAUDE.md)の
「クラウド同期」節を参照。

- Browserペインの実地テストで、strength/holdBased/cardio種目を含むテストセッションが正しく
  Supabaseへ書き込まれること、同じ記録の再送で重複しないこと、不正データが自動リトライで
  自然に回復すること、他アカウント宛てのキューが誤送信されないことを確認
- テスト中に2件のバグを発見・修正（Codexレビューでも指摘されたキュー消失競合、Claude自身の
  実地テストで追加発見した「ロック中の新規エントリが送信されないまま放置される」問題）
- 実機（iPhone）でのオフライン→オンライン復帰の実地確認、ログアウト時のキュー実地テストは
  未実施（フェーズ6の実機総合検証で行う）
- フェーズ1から持ち越していた「別アカウントでのRLS実地テスト」はユーザーと合意の上見送り

## 2026-09-07: クラウド同期(js/sync.js)を追加

`game-daily-manager`（別リポジトリ）と接続する統合作業の一環。詳細設計・既知の制約は
[CLAUDE.md](CLAUDE.md)の「クラウド同期」節を参照。実装内容:

- 新規ファイル`js/sync.js`（Supabase接続・認証状態管理）
- 初回起動時の同期選択モーダル（`index.html`の`#sync-choice-modal`、`js/app.js`の
  `wireSyncChoiceModal`、`js/ui.js`の`openSyncChoiceModal`等）
- 記録画面「その他の設定」にクラウド同期の状態表示・ログイン/ログアウトボタン
  （`js/ui.js`の`renderSyncStatus`）
- Codexレビューで見つかった不備（ログアウト失敗時のエラー無視、設定画面側ログインボタンの
  エラーフィードバック欠如、`redirectTo`にクエリ/ハッシュを含めていた点）を修正済み
- 実機（Chrome、通常のブラウザタブ）でGoogleログイン成功・クラウド同期状態の表示まで確認済み
- **未確認**: ホーム画面に追加したPWA(standalone起動)でのOAuthリダイレクト挙動。次回、実機で
  ホーム画面から起動した状態でのログインを確認すること
- 同じセッション内で、無関係な未コミット変更（体重ラベルのバグ修正）を発見し、別コミットとして
  分離済み（`70f77cd`）

## 次の作業

ショートカット機能まわり（クイックスタート・`?view=record`・削除のクラウド伝播）は実機確認・
Codexレビューの反映まで完了。Codex修正版（entryId化・URL排他処理）自体の実機再確認は未実施。
次回、週間プランを全体管理画面へ表示する機能に着手する場合は、このショートカット機能の設計
（`link_type`カラムでの拡張余地）を踏まえること。

## 過去の作業（〜2026-08-14、v31まで）

## 今回のセッションでやったこと（すべてpush済み、v9〜v31）

1. **今日の予定パネル**: モード選択画面の最上部に週間プランの「今日の予定」を表示（原点回帰UX
   見直しの一項目）。実機バグ（`<details>`の非表示処理をSafariが上書きする問題）を発見・修正済み。
2. **控えめな操作ボタンの見た目統一**: 「他のメニューを作る」を皮切りに、箱スタイル(`.wu-cd-toggle`)や
   下線リンク(`.text-link-btn`)だった9箇所を`.ghost-pill-btn`（輪郭だけの角丸pill）に統一。
   完了トグルは最終的にピルボタン化（`.done-toggle-pill`）。死んだCSSも削除済み。
3. **今日の予定パネル・今日のヒントの再調整**: ベタ塗り箱だった2箇所を、`.section-toggle`と同じ
   「箱を作らず左にアクセント線」スタイルに変更。
4. **器具絞り込みノートの文言修正**: 画面をまたいで「②」という設定画面の見出し番号を参照していて
   分かりにくかったため、「設定画面で選んだ器具のみ表示中」に変更。
5. **セット完了時の固定＋行の縮小**: 完了にした後もスライダー/ホイールが操作できてしまう問題を修正
   （`disabled`化）。完了にすると行が縮み、「10回・RPE7」のような1行サマリーを表示する。
6. **回数/RPE/休憩時間を数字ホイールに、体重を初回手入力+以後ホイールに変更**（最終的な結論）:
   重量(記録画面の挙上重量)・有酸素の時間/距離は`<input type="range">`スライダーのままだが、
   それ以外は何度もスライダー側の見た目を調整しても収まりが悪く、「範囲固定・横スクロールで
   数字を選ぶ」数字ホイール方式に切り替えた（`numberWheelTrackHtml`/`wireNumberWheels`）。
   体重だけは「一度決めたら使い続ける値」という性質のため、未設定の最初の1回だけ数字入力
   フォームを出し、確定後はホイールに切り替わる。以前あった「端で範囲が伸びる」仕組み
   （体重・回数どちらも）は不要になり削除、範囲は全て固定にした。
   - 試したが最終的に不採用になったもの: 目盛り線（体重・回数・RPE全て）、±1/±5/±10の
     ステッパーボタン（複数の見た目案を検討したがしっくり来ず）。いずれも「ごちゃついた」との
     指摘で撤回・不採用。
   - 参考にした実例: Strong/Hevyという実際のワークアウト記録アプリ2つも見たが、どちらも
     「数字をタップして直接入力」方式で、このアプリの「操作して選ぶ、直接入力させない」という
     既存方針とは合わないため採用しなかった。
   - **検証中に見つけた副産物のバグ修正**: 数字ホイールの初期位置合わせが
     `requestAnimationFrame`任せだったため、Browserペインが非表示で描画が止まっている状況
     （検証用タブが裏に回っている等）ではrAFが発火せず、初期位置合わせが永久に行われないことが
     あると判明。レイアウト取得はrAFを待たず同期的に行えるため、rAF依存をやめて同期呼び出しに
     変更した（`initNumberWheel`）。実機・通常のアクティブなブラウザでは元々問題無かったはずだが、
     この修正でより頑丈になった。
   - **実機で発覚した重大バグと修正**: pushした版で「自分で作る画面が横にスクロールできる」
     「体重の設定が何もできない」という報告があった。原因は体重ホイールの範囲が20〜200kg(360個)と
     非常に横長で、その内容の最小幅がそのまま`<fieldset>`(`.field-group`)に伝わり、fieldset自体が
     ブラウザの既定挙動（幅指定が無くても"中身の最小幅"まで自動的に広がる、古いHTML/CSS仕様の
     挙動）でページ幅を超えて広がり、画面全体が横スクロールできる状態になっていたこと。
     `.field-group`に`min-width: 0`を追加して打ち消し、`.number-wheel`/`.number-wheel-track`にも
     `width: 100%`・`min-width: 0`を明示して防御的に固めた。**教訓**: 中身の要素数が多い
     (=横に長くなりうる)UI部品を追加する時は、`<fieldset>`のような特殊なUA既定挙動を持つ要素が
     祖先に無いか確認すること。またPCプレビュー検証時、ブラウザのCSS/JSキャッシュが`navigate`の
     クエリパラメータだけでは更新されず古いスタイルシートを見続けてしまうことが複数回あった。
     `navigate`に`force: true`を付けるか、`fetch({cache:'no-store'})`でサーバー側の実際の内容を
     直接確認して切り分けること。

各項目の設計判断・データモデルの詳細は[CLAUDE.md](CLAUDE.md)の該当節（「モード選択画面：今日の
予定を最上部に出す」「控えめな操作ボタンの見た目統一」「記録画面の入力方式」）を参照。デザイン案の
比較検討はすべてmcp__visualize（Imagine）で複数案を実際の配色・実際のUIコンポーネントとして提示し、
ユーザーに選んでもらう進め方で行った。

## 確認方法（Claudeが実施・完了）

- `node --check`で`js/app.js`・`js/ui.js`の構文エラー無しを都度確認。
- PCプレビュー（`training-menu-alt`設定、ポートは都度自動割り当て）で、localStorageに直接テスト
  データを入れて全項目を実際にその画面まで遷移して確認（スクリーンショット、DOM状態のJS確認、
  ドラッグ/タップのシミュレーションなど）。
- PCプレビュー側でService Workerの古いキャッシュを掴んで変更が反映されない事象が何度も発生した
  （`unregister()`+`caches.delete()`で都度クリアして対処。実機側でも同じ注意が必要）。
- 数字ホイールの検証中、テスト用に作った不完全なセッションオブジェクトで`currentSession`を
  一時的に壊してしまい、その状態のまま確認して見かけ上のバグ（RPE残りレップ表示が更新されない）に
  見えたことがあった → ページを再読み込みしてクリーンな状態からやり直したら問題無しと確認できた。
  次回似た調査をする時は、テスト用に`currentSession`を直接差し替える操作をしたら、その後は
  必ずページをリロードしてから本来の確認に戻ること。
- 数字ホイールの中央ハイライト枠がボトムナビの上に描画されるバグは、実機だけでなくPCの通常の
  Chromeでも再現していた（ユーザーが`localhost`のプレビューURLを直接開いて発見）。Claude自身の
  検証では「1個の要素の高さ」だけ確認して満足してしまい、複数要素の位置関係や重なり順
  （z-indexのスタッキング）までは見ていなかった。今後、position:fixedの要素(ボトムナビ等)と
  position:absolute/z-index付きの要素が同じ画面にある変更をした後は、実際にスクロールした状態の
  スクリーンショットで重なりが無いか目視確認すること（数値のgetBoundingClientRectだけで
  「直った」と判断しない）。

## 実機（iPhone）確認状況

- **確認済み**: モード選択画面最上部（今日の予定パネル・「他のメニューを作る」）。
  screenshot-relay経由のスクショ＋「良い感じだと思う」の言葉で確認済み。
- **v31を実機で確認済み**: 「自分で作る画面の横スクロール」「体重の設定ができない」の2件を
  ユーザーが実機で報告→fieldsetのmin-width問題と特定・修正→v31をpush→ユーザーが実機で
  再確認済み。
- **⚠️ 次のチャットで続きの指摘がある**: v31確認後、「まだ直したいところがある」とのことで、
  **具体的な内容は次の会話で改めて伝えられる**（この時点では内容不明・Claude側から先回りして
  推測・着手しないこと）。次のセッションでは、まずユーザーに何を直したいか確認してから着手する。
- **その他、まだ実機で確認できていない箇所**: 見た目統一の残り、今日のヒント、器具絞り込み文言、
  完了ボタン・行の縮小、数字ホイール全般の細かい操作感（スナップの気持ちよさ、タップの精度）。
  体重は初回セットアップ時にしか手入力フォームが出ないため、確認するには一度`localStorage`を
  クリアするか、別のブラウザ/シークレットモードで開く必要がある。

## 未確認事項・既知の問題

- **最優先**: 上の「実機（iPhone）確認状況」に書いた、次のチャットで伝えられる予定の指摘内容。
- その他、上記の実機未確認の項目全般。
- 原点回帰の残り項目（チュートリアル作成、「今日やることを最上部に」以外のChatGPT案、動画/デモ機能）
  は未着手のままObsidian Vaultのロードマップに残っている。

## 参照

- [CLAUDE.md](CLAUDE.md)
- [AGENTS.md](AGENTS.md)
- Obsidian Vault「アイデアまとめ\03 トレーニングメニューアプリ\ロードマップ.md」
