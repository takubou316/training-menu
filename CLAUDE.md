# CLAUDE.md

このファイルはClaude Code（claude.ai/code）がこのリポジトリで作業する際のガイドです。

## アプリ概要

自分専用の筋トレメニュー自動生成＋記録PWA。ビルドステップなし・依存ライブラリなしのvanilla HTML/JS/CSS。
部位・使える器具・時間・レベル・目的を選ぶだけで、あらかじめ用意した種目データベースとルールベースのロジックからその日のメニュー（ウォームアップ〜本編〜クールダウン）を組み立てる。
**実行時にAI(LLM)は一切呼び出さない**（画像メモの「AIに文を書かせるとAI過ぎるから作らせたくない」という方針のため）。トレーニング記録はlocalStorageに保存し、次回同じ種目をやる時に前回実績を踏まえた重量/レップの提案（プログレッシブオーバーロード）を出す。

## HANDOFF.mdについて

このCLAUDE.mdは長期の設計方針・注意点を置く場所。**今どこまで進んでいて次に何をするか**は、同じディレクトリの`HANDOFF.md`にまとめる（ClaudeとCodex併用運用の引き継ぎメモ、詳細はルート`CLAUDE.md`の「Claude/Codexの併用について」参照）。コード・設定に変更を加えたら作業終了時に必ず更新する。

## v1のスコープと今後の相談事項

最初のバージョンは「筋トレメニュー自動生成＋記録」のみ。画像メモにあった以下は次のステップとして別途相談してから着手する:
- 継続日数の記録・表示（何日続いているか）
- 体調の悩みに対するエビデンスベースのアドバイス
- 有酸素運動・歩数・ウォーキングの記録（ヘルスケア連携含む）
- ウォームアップ/クールダウンの高度なカスタマイズ（簡易版は本編メニューに同梱済み。各動的ウォームアップにⓘ説明と対象種目名の紐づけあり）
- **ウォームアップの「なぜやるか」は`js/menu-generator.js`の`DYNAMIC_WARMUP_BY_PATTERN`に書いてあるが「どうやってやるか（具体的なやり方）」の説明が不足しており分かりにくいとの指摘あり。**
  2026-08-09、ウォームアップに短い静的ストレッチ(下記)を追加したことで「具体的に何をするか」が
  一部補われたが、`DYNAMIC_WARMUP_BY_PATTERN`自体の各動きの説明文はまだ「なぜやるか」中心のまま。
  完全な解決ではない点に注意。

## 気になる部位・痛みの事前ヒヤリング

設定画面の最上部で「肩・腰・膝・手首」の気になる部位を選べる（複数選択可、デフォルトは「特になし」）。
選択すると`js/exercises-data.js`の各種目の`riskAreas`と照合し、負担がかかりやすい種目を`js/menu-generator.js`の
`filterByPainAreas`で事前に除外してからメニューを組み立てる。医学的な診断・アドバイスではない一般的な目安である旨を
メニュー画面に注記として表示している。

## ウォームアップ・クールダウンの中身の組み立て方（`js/menu-generator.js`の`buildWarmupAndCooldown`）

出典は『健康運動実践指導者 養成用テキスト』第8章A（`docs/knowledge/warmup-cooldown-stretching.md`参照）。
教科書のウォームアップは「①軽い有酸素運動→②関節を動かす体操→③主要部位の10秒程度の短いストレッチ」の
3段階構成とされており、②までは元々実装済みだったが、2026-08-09に③を追加した:
- `STATIC_STRETCH_BY_MUSCLE`（クールダウン用、保持時間20〜30秒）のエントリを`toWarmupShortStretch`で
  「10秒」表記に変換して流用している。新しくストレッチ内容を作らず、同じ内容・同じやり方で保持時間だけ
  短くするという教科書の考え方に沿ったもの。`warmup.staticStretch`として、動的ウォームアップ(`warmup.dynamic`)の
  直後・ウォームアップセットの案内の前に表示される。

クールダウンのストレッチ(`cooldown.static`)の並び順にも同日に優先順位付けを追加した。教科書は
「疲労感の強い部位／過去に傷害歴のある部位」を優先すべきとしているが、これらは本人の主観申告が
必要な情報でありUIを増やさずには取得できないため、以下の代理指標で近似している（`buildWarmupAndCooldown`の
第2引数`painAreas`）:
1. 気になる部位設定(painAreas、肩/腰/膝/手首)に近い部位のストレッチを最優先
   （`PAIN_AREA_TO_STRETCH_MUSCLES`による近似マッピング。該当種目自体は既に`filterByPainAreas`で
   除外済みだが、周辺部位のケアとして優先する意図）
2. その次は、このセッションで主動筋として使われた回数が多い部位ほど先（疲労感の強さを使用頻度で近似）

「要望から作る」はメニュー画面で`currentMenu.params.painAreas`をそのまま渡せるが、「自分で作る」には
気になる部位の選択UIが無いため、設定画面で最後に保存された`loadSettings().painAreas`を代わりに使う
（一時的な条件ではなく本人の恒常的な特性に近いという判断）。

## ウォームアップセットについて

コンパウンド種目には`js/rules.js`の`LEVELS[level].warmupSets`に応じたウォームアップセットを本セットの前に追加する
（`js/workout-log.js`の`createSessionFromMenu`で`isWarmup: true`のセットとして生成、提案重量の50%目安）。
記録画面ではセット番号の代わりに「ウォームアップ：軽い動作で数回」という案内文を表示し
（以前は「W1」と番号だけ出していたが何をすればいいか分かりにくいと指摘があり案内文に変更、
さらに案内文だけだと本セットと見間違えるとの指摘があり、色つき背景・左のラインでも
はっきり区別できるようにした。`.set-row-warmup`）、履歴・前回実績・
総挙上量の計算からは除外される（`js/storage.js`の`findLastPerformance`と`js/workout-log.js`の
`computeSessionVolume`で`isWarmup`を除外）。

## 自重種目の重量について

自重種目（バーベル/ダンベル/マシンを使わないもの）は「重量」という概念がユーザーにわかりにくく、
かつプッシュアップのように体重の一部しか負荷になっていない種目もあるため、重量スライダーは表示せず
体重スライダーから自動計算する（`js/workout-log.js`の`isBodyweightLoadExercise`/`buildSuggestion`）。
プッシュアップ系のみ研究に基づく負荷率(`bodyweightLoadFactor`、`js/exercises-data.js`)を設定し、
それ以外の自重種目は体重100%をそのまま負荷とみなすフォールバック。記録画面には計算式を含めた
説明文を種目ごとに表示する。

保持時間系(プランク等、`holdBased: true`)は上記の対象外で、「重量」という概念自体を持たない
（プランクの負荷を体重の何%と見積もる根拠のある研究値も見当たらなかったため、無理に数値化せず
重量スライダー自体を出さない方針とした）。`js/workout-log.js`の`buildSuggestion`・
`js/ui.js`の履歴詳細表示ともに`holdBased`を見て保持秒数ベースの文言に分岐しており、
以前はこの分岐が漏れていて「前回0kg×45秒」のように意味のない0kg表記が出る不具合があった。

体重スライダーは「要望から作る」の設定画面(`#bodyweight-slider`)と「自分で作る」画面
(`#bodyweight-slider-custom`)の両方にあり、どちらを動かしても同じ値として同期・保存される
（`js/app.js`の`setBodyWeightKg`）。以前は設定画面側にしかなく、「自分で作る」しか使わない
場合は体重を一度も入力しないまま自重種目の負荷推定(初期値60kg)が行われてしまっていたため追加した。

## タイマーまわりの方針

4種類のタイマーを用途ごとに分けている:
- `js/rest-timer.js`: 記録画面でセットの「完了」にチェックを入れると自動で始まる、全画面表示の休憩タイマー
  （`#rest-timer-modal`）。対象種目の`restSec`ぶんカウントダウンし、「+10秒」で延長、「今すぐ終わる」で
  即座に閉じられる。メニュー作成画面など記録画面以外では呼ばれない。残り3・2・1秒でビープ音、
  0秒で終了の合図音を鳴らす（音声ファイルは使わずWeb Audio APIで生成、`ensureRestTimerAudioCtx`）。
- `js/session-timer.js`: トレーニング開始〜終了の経過時間を裏側で計測するだけで、画面には表示しない
  （記録確定時に`durationSec`として保存し、履歴一覧にのみ表示する）。
- `js/hold-timer.js`: プランクなど回数ではなく保持時間を計測する種目(`exercises-data.js`の`holdBased: true`)専用の
  タイマー。セットの「秒」スライダー横の▶ボタンで開始すると全画面表示（`#hold-timer-modal`）になり、
  姿勢を作る猶予として5秒の「準備」カウントダウン（ビープ音つき）を挟んでから計測を開始する
  （`HOLD_TIMER_PREP_SECONDS`）。計測中は結果をリアルタイムでスライダーに反映し、「中断」で終了する。
- `js/cardio-timer.js`: 有酸素種目の「時間」スライダー横の▶ボタンで始まるストップウォッチ。全画面表示
  （`#cardio-timer-modal`）で「計測を終わる」を押すまで数える。計測中は「時間」スライダーと推定カロリーを
  リアルタイムで更新する。「休憩」を押すと運動時間の計測を一時停止し、代わりに休憩の経過時間を表示する
  （運動時間はそのまま増えなくなる）。「再開」で運動時間の計測に戻る。休憩の開始時刻・長さは
  `exercise.restLog`に記録し、記録画面(推定消費カロリーの下)と履歴の詳細表示の両方に
  「休憩N回・計M:SS」として表示する（`formatCardioRestSummary`）。「時間」スライダー自体が
  秒単位・1秒刻み(`min=0, max=7200, step=1`)なので、計測中の実測値をそのまま代入でき、
  手でドラッグする時用に別のstepへ切り替えるような処理は不要。

上記4つ全て「開始時刻(`Date.now()`)だけを覚えておき、表示のたびに現在時刻との差分で経過時間を
計算し直す」方式で統一している。ブラウザ/PWAには画面ロック中やアプリ切り替え中もJSを動かし続ける
権限がなく真のバックグラウンド実行はできないが、この方式なら裏での更新が一時的に止まっても
（`setInterval`が遅延・休止しても）、画面に戻ってきた瞬間に正しい経過時間へ復元される
（増分カウント方式だと止まっていた分がそのまま失われる）。OSがアプリのプロセスごと終了させた
場合は他の記録同様にリセットされる点は変わらない。

全画面タイマー(rest-timer/hold-timer)表示中は`js/app.js`の`lockBodyScroll`/`unlockBodyScroll`で
背面ページのスクロールをロックしている。単純に`overflow:hidden`を付けるだけだと、特にiOS Safariで
ロック解除時にスクロール位置が一番上に戻ってしまう既知の問題があるため、ロック時に`window.scrollY`を
覚えておき、bodyを`position:fixed`＋`top:-スクロール量`で固定し、解除時に`window.scrollTo`で
元の位置へ戻す方式にしている（`body.modal-open`、CSS側は`position:fixed`/`width:100%`のみ担当し、
`top`の値はJS側で毎回動的に設定する）。

上記の全画面タイマー(rest-timer/hold-timer/cardio-timer)は表示中、`js/app.js`の
`lockBodyScroll`/`unlockBodyScroll`で背面ページのスクロールを止めている。全画面表示とはいえ
背面のページ自体は`position:fixed`の影響を受けず素通しでスクロール可能なままなので、ロックして
いないとタイマー表示中にスワイプした操作が背面ページを動かしてしまい、タイマーを閉じた瞬間に
記録画面が勝手にスクロールした位置になっている、という不具合があった。

## 記録画面の入力方式

重量・回数(または秒)・RPEはすべて`<input type="range">`のスライダー（数値の直接入力ではなく操作して選ぶ形）。
RPEは`js/rules.js`の`RPE_SCALE`（1〜10、0.5刻み）を使用。これはレジスタンストレーニング向けRPE
（Reps in Reserveベース、Zourdos et al. 2016、NSCA発行のStrength and Conditioning Journal掲載）に基づく。
回数・秒数スライダーはどちらも1刻みで細かく動かせる。回数側は上限(max)だけ常に現在値の1段上まで
（例: 10回なら上限20）にしてあり、右端まで動かして指を離すとさらに+10ずつ上限が伸びる
（`js/ui.js`の`repsInitialMax`、`js/app.js`の`handleLogInput`）。体重スライダーにも同じ考え方を
2026-08-14に適用済み（下記「体重スライダーの範囲」参照）。

## スライダー全般の見た目・使い勝手（2026-08-14）

「見た目が古い・チープ」「微調整しづらい」という指摘を受けて、アプリ内の全スライダー
（`.slider-field input[type="range"]`。体重・重量・回数・RPE・休憩時間・有酸素の時間/距離）に
以下の改善を入れた:

- **見た目**: トラックを太く(8px)・つまみを大きく(26px円)し、現在値までを`--accent`で塗って
  進捗を一目で分かるようにした（以前は`accent-color`だけでブラウザ標準の見た目に任せていた）。
  WebKit(iOS Safari)には`::-webkit-slider-progress`が無いため、塗りは`--slider-fill`カスタム
  プロパティ+トラック背景のグラデーションで表現している（`js/app.js`の`updateSliderTrackFill`）。
- **ドラッグ中の値バブル**: つまみの真上に、隣の`.slider-value`ラベルと同じ表記（例:「87 kg」
  「RPE 7」）を吹き出しで表示する。指を離すと消える（`showSliderBubble`/`positionSliderBubble`/
  `hideSliderBubble`）。
- **配線方法**: 個々の描画箇所(`sliderFieldHtml`等)を1つずつ直す代わりに、`document`レベルの
  イベント委譲(`input`/`pointerdown`/`pointerup`)と`MutationObserver`（`#main`配下に新しく
  スライダーが描画されるたびに自動で塗りを初期化）で一括対応している(`wireSliderEnhancements`)。
  今後スライダーを新設しても、この配線を個別に増やす必要はない。
- **トラック両脇の範囲表示**: 「どこまで動かせばどれだけ数字が動くか分からない」「知らないうちに
  大きく動かしすぎる」という指摘を受け、トラックの両脇に今のmin/maxを薄いラベルで表示する
  （`.slider-track-row`/`.slider-bound-label`）。体重・回数のように端で範囲(min/max)が伸びる
  スライダーは、`updateSliderTrackFill`の中で`updateSliderBoundLabels`も呼ぶことでラベルの
  表示も追従させている。秒数系(休憩時間・有酸素の時間)は分:秒表記に変換して表示する
  （`formatSliderBoundLabel`）。
- **既知の罠（実機のみで発覚）**: 端まで動かして範囲(min/max)を伸ばす処理の直後、塗りつぶし
  (`--slider-fill`)を再計算していないと、伸ばす前の古い割合が残ってしまうバグがあった
  （体重・回数の両方で発生）。範囲を書き換える箇所では必ず`updateSliderTrackFill`もセットで
  呼ぶこと。また体重スライダーは設定画面/自分で作る画面の2箇所で値を同期しているが、
  `.value`を直接書き換えるだけではブラウザ標準の`input`イベントは発火しないため、
  操作していない側は`setBodyWeightKg`内で明示的に`updateSliderTrackFill`を呼ぶ必要がある。

### 体重スライダーの範囲（2026-08-14）

以前は`min=30 max=150`の固定範囲で、0.5kg刻みの微調整がスライダー1本の横幅の中でやりにくい
（1pxあたりの変化量が大きい）と指摘があった。回数スライダー(`repsInitialMax`)と同じ考え方で、
初期表示は現在値の前後15kgだけに絞った狭い範囲にし、端まで動かして指を離すとその方向へ10kgずつ
広げる（`applyBodyWeightSliderBounds`、`wireBodyWeightSlider`内の`change`リスナー）。回数と違い
下限側にも同じ仕組みがある（軽い方に外れた人にも対応するため）。

## ローカル起動

`serve.bat` を実行（`python -m http.server 8081`）し、`http://localhost:8081` を開く。

## アーキテクチャ

- `index.html` — アプリシェル（設定画面/メニュー画面/記録画面/履歴画面/グラフ画面のDOM）
- `css/style.css` — スタイル一式（スマホ幅を想定した1カラムレイアウト）
- `js/exercises-data.js` — 種目データベース（部位・カテゴリ・必要器具・動作パターン・初心者向けフォーム解説文`description`・負担がかかりやすい部位`riskAreas`）
- `js/rules.js` — 目的別セット/レップ/休憩の基準値、レベル別セット数、時間から種目数を出す関数
- `js/menu-generator.js` — 部位/器具/時間/レベル/目的からメニュー（W-up/本編/クールダウン）を決定的に組み立てる純粋関数
- `js/storage.js` — localStorageへの設定・記録の永続化、直近実績の取得
- `js/workout-log.js` — 生成メニューから記録用セッションを作成し、前回実績を踏まえた提案を計算、記録確定処理
- `js/ui.js` — DOM描画（状態は持たない）
- `js/session-timer.js` — トレーニング全体の経過時間を裏側で計測（画面表示なし）
- `js/hold-timer.js` — プランク等の保持時間計測用の手動ストップウォッチ
- `js/app.js` — 起動処理・状態管理・画面遷移・各モジュールの結線（エントリポイント）
- `manifest.json` / `service-worker.js` — PWA化（ホーム画面追加、静的アセットのオフラインキャッシュ）

## 有酸素種目

`exercises-data.js`の`type: 'cardio'`を持つ種目（ウォーキング・ランニング・自転車(屋外/室内)・
エリプティカル・ローイングマシン・縄跳び・階段昇降・水泳）は、「セット×回数×重量」という
強度種目のデータ構造に根本的に合わないため、別の記録方式にしている:
- 記録するのは**時間(秒単位・1秒刻みで持ち、「X分Y秒」で表示する。`js/ui.js`の`formatMinSec`)・
  距離(km、屋外でGPS等により実測できる種目のみ)**。重量・セットという概念はない。
  以前は「きつさ」としてRPEスライダーを流用していたが、レジスタンストレーニング向けRPE
  (Reps in Reserveベース)を有酸素に転用すること自体が概念として合っておらず、カロリー計算の
  強度補正も根拠のない自作の式だったため撤廃した（種目ごとのMET値の違いで強度差はある程度表現される）
- **推定消費カロリー**を`体重×MET×時間`で表示する（`js/workout-log.js`の`estimateCardioCalories`。
  MET値は2024 Adult Compendium of Physical Activities(Ainsworth et al.)の中強度時の代表値）
- 自動生成（要望から作る）のアルゴリズムには組み込んでいない（部位・動作パターンベースの選定と
  噛み合わないため）。「自分で作る」画面・「今日のメニュー」の＋種目を追加、どちらからも手動で
  追加できる（`js/menu-generator.js`の`buildCustomCardioPlan`）
- 種目ピッカーでは検索中でも見分けられるよう、部位ラベルの代わりに「有酸素」バッジを表示する
  （`.picker-item-cardio-badge`）
- 進捗グラフは、距離が測れる種目は距離の推移、それ以外(室内マシン系)は時間の推移を表示する

## メニューの作り方：「要望から作る」と「自分で作る」

最初の画面（`screen-mode`）で2択にしている:
- **要望から作る**（従来通り）: `screen-setup` → `generateMenu()`。生成後の`screen-menu`でも
  種目の追加（＋種目を追加ボタン→種目ピッカー）・削除（✕）・並べ替え（↑↓）ができ、変更するたびに
  ウォームアップ/クールダウンを`buildWarmupAndCooldown()`で再計算する（`js/app.js`の`toggleMenuExercise`/`recomputeMenuWarmupCooldown`）。
- **自分で作る**（`screen-custom`）: 目的・レベルの概念がなく、セット数3・レップ8〜12固定
  （`js/menu-generator.js`の`buildCustomSetPlan`）で、休憩時間だけ種目ごとにスライダーで指定できる
  （デフォルト90秒）。ウォームアップ/クールダウンは選んだ種目から自動計算されるが、個別に✕で除外できる
  （手動編集は次に種目を追加/削除するまでの間だけ有効。追加/削除するとその時点の種目構成から再計算される）。

種目ピッカー（`#exercise-picker-modal`）は上記2つの画面で共有しており、`exercisePickerTarget`('custom'|'menu')
で追加先を切り替えている。EXERCISES配列を名前部分一致で検索できる。

## メニュー生成ロジックの考え方（データの出典について）

種目と筋肉部位の対応関係、目的別セット/レップ/休憩の基準値（筋肥大は8〜12回・休憩60〜90秒、筋力向上は4〜6回・休憩長め、
持久力は15〜20回・休憩短め、など）は、JATI/NSCA/ACSMなど各種資格団体のテキストで共通して示されている一般的な目安を
参考にしてコード内に数値として定義したものであり、特定文献からの丸写しではない。数値基準を変更したい場合は
`js/rules.js`の`GOALS`/`LEVELS`を編集する。種目を追加・修正したい場合は`js/exercises-data.js`の`EXERCISES`配列を編集する。

`level`(初心者/中級者/上級者)は以前セット数・休憩時間にしか反映されておらず、種目選定自体には
一切使われていなかったため、初心者を選んでもバーベルスクワット・デッドリフト・懸垂のような技術難度の
高い種目がそのまま選ばれることがあった。`js/exercises-data.js`の各種目に`minLevel: 'intermediate'`を
付けると、「要望から作る」で初心者を選んだ時だけその種目を候補から除外できる（`js/menu-generator.js`の
`filterByLevel`）。対象は技術習得の難度が高いバーベル種目・大半の初心者が1回もできない懸垂・種目の
説明文自体が難度の高さに触れている種目など、控えめな基準で絞った一部の種目のみ（「自分で作る」は
種目を自分で選ぶモードなので対象外）。

`pickTargetedExercises`（部位を絞った時の種目選定）には、候補が使用済みで配列から減っていくと
ラウンド番号とインデックスがズレて本来選べるはずの候補を飛ばしてしまうバグがあり、条件を絞ると
実際の候補数より少ない種目しか選ばれないことがあった。部位ごとに専用のポインタを持たせて修正済み。
また、器具・レベル・部位などの条件が絞られすぎて目安の種目数(`exerciseCountForTime`)に届かなかった
場合は、メニュー画面に理由を添えた注記を表示する（`generateMenu`が返す`requestedCount`と
`js/ui.js`の`renderMenu`内の`shortfallNoteHtml`）。

条件を絞ると種目が1〜2個しか選ばれないパターンをさらに減らすため、以下も行った:
- `js/rules.js`の`TIME_TO_EXERCISE_COUNT`の下限を2→3に引き上げ（15分でも最低3種目は目指す）
- `pickTargetedExercises`は、主動筋(primary)だけで目安数に届かない場合、補助筋(secondary)として
  関与する種目もフォールバックで候補に加える（`fillTargetedRoundRobin`を主動筋→補助筋の順で2回呼ぶ）
- `js/exercises-data.js`の種目データ自体も、部位×器具の組み合わせで候補が0件だった手薄な箇所
  （自重の二頭筋・ハムストリングス、バーベルの三頭筋）を埋める種目を追加した
  （チンアップ`chinup`、タオルレッグカール`towel_leg_curl`、バーベルライイングトライセプス
  エクステンション`barbell_skull_crusher`）。あわせて`hip_thrust`はバーベルでも行える種目のため
  equipmentにbarbellも追加した。それでも0件になりうる組み合わせ（バーベルのみ×ふくらはぎ等）は
  残っており、その場合は上記のshortfallNoteHtmlか「見つかりませんでした」エラーで案内する。

## 進捗グラフ

チャートライブラリは使わず、`js/ui.js`でインラインSVGを自前生成している（ビルドステップなし・
依存ライブラリなしの方針のため、CDN読み込みも避けた）。

種目ごとの進捗指標は、種目のタイプによって出し分けている（`js/ui.js`の`progressMetricInfo`）:
- **重量を設定するタイプ(ダンベル/バーベル/マシン)**: そのセッションで一番重かったセットの重量。
  その時の回数はツールチップの内訳として見られる
- **自重種目(プッシュアップ等)**: 重量という概念がわかりにくいので、一番多くできた回数
- **保持時間系(プランク等)**: そのセットで一番長く保持できた秒数

以前は「推定1RM(Epley式)」で重量と回数を1つの数値にまとめていたが、Epley式は本来コンパウンド種目の
低レップ(〜10回程度)向けの推定式で、高レップのセットやアイソレーション種目では誤差が大きく
当てにならないと分かったため、種目タイプ別に素直な実測値を見せる方式に変更した(`js/workout-log.js`の
`exerciseProgressSeries`)。以前あった`overallVolumeSeries`（セッション全体の総挙上量を時系列にする）は
種目をまたいだ重量×回数の単純合算(スクワット65kg×15回とプランクを足す等)に生理学的な意味がほぼ無く、
「その日やったか」自体はカレンダー(記録タブ)側で既に分かるため、2026-08-14に関数ごと削除した。
- 記録画面: 各種目カードに`buildProgressSparklineHtml`の小さいスパークライン（軸なし、
  直近値だけをテキストで直接ラベル表示）。データ点が2つ未満の種目（初回など）は表示しない。
  あわせて、その場でも分かる「本セットの回数(または保持秒数)が1セット目から今のセットまで
  どう変わったか」（`buildRepsProgressionText`、スライダー操作でリアルタイム更新）と、
  「このセッション内でより前に同じ主動筋を使う種目をやっていないか」の注記
  （`priorSameMuscleOverlap`/`buildPrefatigueNoteHtml`。疲労で回数・重量が下がっていても
  それが「先に同じ筋肉を使ったから」だと分かるように）を表示する。
- **グラフタブ**（`#screen-record`内、上位タブ「記録／グラフ」の「グラフ」側）: 2026-08-13に
  記録一覧と統合されて`#screen-history`/`#screen-progress`という別画面ではなくなった
  （詳細は下記「記録画面：カレンダー統合」参照）。`<select>`で種目を選んで見る種目別の推移のみ
  （かつて上に表示していた全体の総挙上量推移は2026-08-14に削除、理由は直上の段落参照）。
  `buildProgressTrendChartHtml`で軸・グリッド線つきの折れ線グラフを描画し、
  各点をタップすると日付と値(重量の場合は回数の内訳も)のツールチップが出る
  （タップ処理は`js/app.js`の`#main`共通クリックハンドラ内、`.chart-point`）。
  種目選択肢は記録履歴に登場する種目だけ（`js/ui.js`の`exercisesWithHistoryOptions`）。

## 記録画面：カレンダー統合（2026-08-13）

「記録一覧」画面と「グラフ」画面を`#screen-record`に統合し、ボトムナビを5個→4個に減らした
（`メニュー作成／週間プラン／記録／豆知識`）。記録タブは📅カレンダー表示／☰リスト表示を
切り替えられる。実装前に`prototype-history-calendar.html`（ダミーデータ試作、削除済み）で
UIを検証しており、以下はその試作で固まった設計判断（実装後も有効な理由づけ）:

- **記録した日の印は緑の小さいドットではなく、日付マスいっぱいのスタンプ画像
  （`assets/stamp-record.svg`）を使う。** 最初はドットとスタンプの複数パターンを実機で
  見比べ、「達成感が一番あるのはスタンプ」という判断だった（2026-08-10、丸パターン2採用）。
  「記録した日が赤だらけになる」ことは問題視せず、むしろ積極採用している。
- **「今日」の強調は、マス全体を枠線で囲むのではなく左上の日付バッジを強調する。**
  スタンプ画像自体が既にギザギザした縁を持つため、マス全体の枠線も付けると縁同士が
  ぶつかって見た目がごちゃつく（実機で指摘あり）。スタンプの中央には既に白地に赤文字
  （「たいへんよくできました」）が入っているため、日付の数字はスタンプに重ねず左上の
  小さいバッジとして表示する。
- **日の詳細（`day-detail-inline`）のヘッダーは`position: sticky`で画面内に留める。**
  種目数が多いセッション（例: 10種目）だと本文が縦に長くなり、◀日付▶の移動ボタンが
  スクロールで画面外に出てタップできなくなるため。`top`の値は`js/ui.js`の
  `updateStickyHeaderOffset()`が`.app-header`の実高さ（`safe-area-inset-top`等で変わる）
  から動的に算出し、CSS変数`--sticky-header-offset`に反映する。リスト表示側
  （`#list-view-container .day-detail-header`）は◀▶ボタン自体が無いためsticky化を無効化
  （`position: static`）している。
  **注意**: この仕組みをposition:stickyで機能させるには、`.day-detail-inline`（sticky要素の
  祖先）に`overflow: hidden`を付けないこと。付けるとsticky可動域がその要素の高さ内に
  制限され、スクロールしてもヘッダーが画面に留まらなくなる（試作時に一度ハマった実例）。
- **記録が1件も無い（初めて使う人）場合の空状態**は「まだ記録がありません」だけで終わらせず、
  「5分でも運動を始めてみませんか？」＋「＋ メニューを作る」ボタンで実際にメニュー作成画面
  （`mode`）へ遷移させる。記録0件時はカレンダー（空マスが並ぶだけで伝わりにくい）ではなく
  初期表示をリスト表示にする。
- **1日に複数回記録がある場合**は、日付ごとにセッションカードを新しい順に積み重ねて表示する
  （試作は1日1件想定だったが、実データは複数あり得るため本実装で追加対応）。

なお、種目名をタップしてグラフタブのその種目の推移へ直接飛ぶ導線（試作の`ex-name-list`／
「推移を見る›」の仕組み）は、試作段階のアイデアとして`css/style.css`に部品だけ残っているが
本実装では未接続。将来また記録カード自体を軽量化（種目名だけ→タップで詳細）したくなったら、
この部品とテキストラベル併記の方針（UI_UX_GUIDELINESの「アイコン単体に意味を頼らない」）を
再利用できる。

## モード選択画面：今日の予定を最上部に出す（2026-08-14、原点回帰UX見直し）

「運動はしたいが何をすればいいか分からない人(A)」と「知識はあるが毎回作るのが面倒な人＝自分自身(B)」
のうちBさん向けの改善。以前は週間プランの「今日」の行が`#weekly-plan-section`パネルの中（「要望から
作る」「自分で作る」の2枚のカードより下）に埋もれており、週間プランがある日でも無関係な2枚のカードを
毎回先に見せられていた。以下の設計で最上部に昇格させた:

- `index.html`の`#today-focus-section`（画面最上部、`<h2>`の直後）を`js/ui.js`の`renderTodayFocus`が
  埋める。週間プランを1つも作っていなければ何も表示しない（従来通り2枚のカードが最初に見える体験を
  変えない）。プランがあれば、今日の予定が実行可能な内容（部位、または削除されていないテンプレート）
  なら「今日は○○の日です」＋「始める」（`weeklyDayContentText`を流用、実体は`startTodayFromActivePlan`）
  を表示し、休みの日なら「今日は休みの日です」という軽い一言だけを表示する。
- 「要望から作る」「自分で作る」の2枚のカードは`#mode-cards-details`という`<details>`（見た目は
  `.ghost-pill-btn`、詳細は下記「控えめな操作ボタンの見た目統一」節参照）に格納した。今日が
  実行可能な予定の日だけ折りたたみ状態（`<summary>他のメニューを作る</summary>`）にし、それ以外（週間プラン未作成／休みの日）は
  `.mode-cards-flat`クラスでsummary自体を非表示にして強制的に開いた状態にする＝見た目は変更前と
  同じ「カード2枚がそのまま並ぶ」に戻る（「他に」何も無い/優先すべき提案が無いのに開閉可能に
  見せる意味が無いため）。
- `#weekly-plan-section`側の週間プラン一覧（`weeklyPlanDaysHtml`）からは今日の行を除外した
  （上の`#today-focus-section`と重複するため）。今日の割り当てだけがある状態で他の曜日が
  全て休みの場合、一覧が空になって「まだ何も割り当てていません」と出ると紛らわしいため、
  その場合だけ「今日以外はまだ割り当てていません」に文言を分けている。
- 見た目は当初`--accent-dim`のベタ塗り箱にしていたが、下記「控えめな操作ボタンの見た目統一」で
  他の操作群を輪郭だけの`.ghost-pill-btn`に揃えた結果、ここだけベタ塗りで浮いて見えると指摘があり、
  `.section-toggle`と同じ「箱を作らず左にアクセント線」のスタイルに変更した（`.today-focus-panel`、
  2026-08-14同日中）。「始める」ボタンだけはその日の主要な行動なので、線の色を`.section-toggle`の
  `--accent-dim`より明るい`--accent`にして少しだけ優先度を示している。
- 原点回帰の残り（チュートリアル、動画/デモ、「今日やることを最上部に」以外のChatGPT案）は
  Obsidian Vault「アイデアまとめ\03 トレーニングメニューアプリ\ロードマップ.md」の
  「これから」欄に引き続き残っている。

**既知の罠（2026-08-14、実機でのみ発覚）**: `<details>`の中身を閉じている間隠すのを
ブラウザ標準の挙動任せにしないこと。`.mode-choice`に`display: flex`を直接指定しており、
一部ブラウザ（iPhone Safari実機で確認、PCのChromiumベースのプレビューでは問題が
再現しなかった）ではこの指定が「閉じたdetailsの中身を隠す」処理より優先されてしまい、
`open`属性が無い(閉じている)のにカードが表示されたまま残っていた。`.mode-cards-toggle:not([open]) .mode-choice { display: none; }`
のように、閉じている時の非表示を自前のCSSで明示することで解決した。同じ構造
（`display`を直接指定した要素を`<details>`の直下に置く）を今後追加する時は同様の
罠に注意する。ブラウザ間の描画差はPCプレビューだけでは気づけない典型例
（[[feedback_phone_real_device_verification]]参照）。

## 控えめな操作ボタンの見た目統一（2026-08-14）

「他のメニューを作る」の実装をきっかけに、アプリ内の「控えめな操作」（もっと見る系の折りたたみ、
下線付きテキストリンク）の見た目がバラバラで、かつ個々の見た目自体も「スタイリッシュじゃない」と
指摘されたため、`.ghost-pill-btn`（輪郭だけ・塗りつぶし無しの角丸pillボタン。`background: var(--surface)`、
`border: 1px solid var(--text-dim)`、`color: var(--text)`）に統一した。決定までの経緯:

1. 「他のメニューを作る」を重い箱スタイル(`.wu-cd-toggle`)→下線付き色文字リンク(`.text-link-btn`風)→
   `.ghost-pill-btn`の順で試行錯誤（詳細は上の「モード選択画面：今日の予定を最上部に出す」節）
2. 気に入られたため、mcp__visualize（Imagine）で実際のダークテーマ配色を使ったモックアップを
   複数案並べて提示し、対象箇所ごとにユーザーが選ぶ、という進め方に移行。以下は最終的に
   `.ghost-pill-btn`（または派生）で統一した箇所:

| 箇所 | 元の見た目 | 変更後 |
|---|---|---|
| 「他のメニューを作る」（モード選択画面） | `.wu-cd-toggle`箱 | 幅いっぱいの`.ghost-pill-btn` |
| 「編集する」「＋新しいプラン」（週間プランパネル） | `.text-link-btn`下線リンク | 幅いっぱいを2等分した`.ghost-pill-btn`×2（`.weekly-plan-links`） |
| 「ほかのプランを見る（N件）」（週間プランパネル） | `.wu-cd-toggle`箱 | 幅いっぱいの`.ghost-pill-btn` |
| 「保存した組み合わせから読み込む」（自分で作る画面） | `.wu-cd-toggle`箱 | 幅いっぱいの`.ghost-pill-btn` |
| 「前回の記録へ／次の記録へ」（カレンダー詳細、`.jump-links`） | `.text-link-btn`下線リンク | 幅いっぱいを2等分した`.ghost-pill-btn`×2 |
| 「セットの詳細を見る」（記録カード内、`.detail-toggle-btn`） | `.text-link-btn`下線リンク | 中身の幅だけの`.ghost-pill-btn`（カードごとに繰り返し出るため他より一回り小さいコンパクト版、`font-size:12px; padding:6px 12px;`） |
| 「この組み合わせを保存」（自分で作る画面、`.template-save-row`） | `.text-link-btn`下線リンク | 中身の幅だけの`.ghost-pill-btn`（右寄せ、通常サイズ） |
| 器具絞り込みトグル「すべて表示する／器具で絞り込む」（種目ピッカー、`.exercise-picker-equipment-note`） | `.text-link-btn`下線リンク | 中身の幅だけの`.ghost-pill-btn`（ノート行に収まるコンパクト版、`font-size:12px; padding:6px 12px;`） |

**ウォームアップ/クールダウン（記録画面）だけは例外的に別スタイル。** これらは「もっと見る」的な
軽い操作ではなく記録画面本編の1セクションなので、`.section-toggle`という専用クラスにした
（箱で囲まず、左にアクセントの縦線`border-left: 3px solid var(--accent-dim)`＋太字タイトル＋
開閉を示すシェブロン`▾`/`▴`。`.section-toggle[open] .section-toggle-chevron`で回転させる）。
`js/ui.js`の`renderLog`内、`<summary>`の中に`.section-toggle-title`と`.section-toggle-chevron`の
2つのspanを分けて入れている。

**実装上の注意（Safari対策）**: `<details>`直下の子要素に`display`プロパティを直接指定していると、
一部ブラウザ(iPhone Safari)で閉じていてもその子要素が表示されたままになる不具合がある
（詳細は上の「モード選択画面」節の「既知の罠」参照）。`.mode-choice`（`display:flex`）だけがこの
問題を踏んでいたため`:not([open])`の明示ルールで対処済みだが、`.menu-block`（`.wu-cd-toggle`の
中身）には`display`指定が無く問題無し。今後`<details>`直下に新しい要素を追加する際、その要素に
`display`を直接指定するなら、念のため`親details:not([open]) 子要素 { display: none; }`も追加すること。

## 記録データの削除

記録一覧画面(`screen-history`)の一番下に、間違って触らないよう`<details>`で畳んだ
「その他の設定」を置き、開くと初めて「記録データをすべて削除する」という控えめな
テキストリンクが出てくる（`.danger-zone`/`.danger-link-btn`、通常のボタンのような
目立つ見た目にしていない）。押すと確認モーダル(`#reset-history-modal`)が出て、
「削除する」を明示的に押さないと実行されない二段構えにしてある。削除されるのは
トレーニング記録(`training-menu:history`)だけで、お気に入りや体重などの設定は残る
（`js/storage.js`の`clearHistory`）。

## データの保存場所

すべてのデータ（設定・トレーニング記録）は端末のlocalStorageのみに保存される。サーバー同期は行っていない
（password-vaultのようなGoogle Drive同期は現時点では未実装）。ブラウザのデータを消去すると記録も失われる点に注意。
